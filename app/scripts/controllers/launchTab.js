'use strict';

angular.module('overtaskApp')
  .controller('LaunchTabCtrl', function (){//$scope, $rootScope) {
	 // $scope.instruction = "Please search for your new"+$rootScope.newTabCategory+"site in the URL Bar ^";
  });
